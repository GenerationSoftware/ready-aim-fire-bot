{
	"name": "bot",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --port 8888",
		"start": "wrangler dev --port 8888",
		"test": "vitest",
		"test:run": "vitest --run",
		"test:watch": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"vitest": "^1.3.1",
		"wrangler": "^4.19.1"
	},
	"dependencies": {
		"viem": "^2.30.6"
	}
}
